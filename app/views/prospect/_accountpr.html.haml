- if (params['controller'] == 'prospects') and (template = template_for_current_view).present?
  = render(template, :account => account)
- else
  -# default view
  = render 'prospect/index_brief', :account => account