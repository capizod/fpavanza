- if (params['controller'] == 'capacities') and (template = template_for_current_view).present?
  = render(template, :capacity => capacity)
- else
  -# default view
  = render 'capacities/index_long', :capacity => capacity
